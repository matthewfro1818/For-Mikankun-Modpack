function onUpdate(elapsed)
  --[[if boyfriendName == 'bf-car-bfs' then
		for i=0,7,1 do
			setPropertyFromGroup('boyfriendStrums', i, 'texture', 'NOTE-assets')
		end
		for i = 0, getProperty('unspawnNotes.length')-1 do
			if not getPropertyFromGroup('unspawnNotes', i, 'mustPress') then
				setPropertyFromGroup('unspawnNotes', i, 'texture', 'NOTE-assets');
			end
		end
    end]]
end